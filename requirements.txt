# --- Core Libraries ---
numpy>=1.24.0
pybind11>=2.10.0

# --- Computer Vision ---
# Використовуємо headless версію, щоб не тягнути зайві Qt-залежності через pip,
# оскільки ми використовуємо системний Qt для GUI.
opencv-python-headless>=4.8.0

# --- GUI Note ---
# PyQt5 НЕ включено сюди навмисно.
# На Raspberry Pi його слід встановлювати через apt (див. README),
# оскільки збірка через pip часто ламається або не сумісна з драйверами відео.
